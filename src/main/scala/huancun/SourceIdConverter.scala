package huancun

import freechips.rocketchip.tilelink.{TLMasterPortParameters, TLXbar}

object SourceIdConverter {

  def newClientParams(raw: Seq[TLMasterPortParameters]) = {

//    val inputIds = TLXbar.mapInputIds(inGroup.map(_._2.client))
  }
}
